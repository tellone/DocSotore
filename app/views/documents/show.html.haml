-content_for :title do
  %title Document view

%h2 #{@document.title}

Tags: #{@document.tag_list}
%p 
  =link_to(File.basename(@document.doc_file.path),
    @document.doc_file.url)
 
%p
  %footer
    Uploaded by: #{@user.email}
